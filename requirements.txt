# Artemis V6 — pure wrapper around Claude with web search.
# No search-API providers, no spaCy, no HTML-extraction stack: search belongs to
# Claude's own web_search tool. Nothing here fetches a page itself.
anthropic>=0.70
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.6
python-multipart
# Postgres driver — only needed if you point ARTEMIS_BOARDS_DB_URL at Postgres.
# psycopg2-binary>=2.9
